package helper;

public class PropertyCustomPathHelper extends PropertyHelper {

    /**
     * Aims to override properties file path (input params for input.xml template)
     *
     * @param  propertyPath  property containing custom path to properties file
     */
    public PropertyCustomPathHelper(String propertyPath) {
        super(propertyPath);
    }
}
